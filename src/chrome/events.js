// Extension background events for ease of use and logging

/** Identifier for the `chrome.runtime.onMessage` event  */
export const MESSAGE = "MESSAGE"

/** Identifier for the `chrome.runtime.onInstalled` event */
export const INSTALLED = "INSTALLED"